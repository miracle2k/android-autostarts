[V1] Option to jump to application info / market.

Support grouping by application.

Give experts more information, like the full broadcast receiver class name.

Add support for Locale broadcasts.

Add German localization.

Update our custom colors to be stateful.